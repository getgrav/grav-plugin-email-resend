{
    "name": "getgrav/email-resend-tests",
    "description": "Test harness for the Email Resend plugin. Kept out of the plugin's own composer.json so that the vendor directory shipped with the plugin holds the Resend bridge and nothing else. Symfony Mailer is here at the 5.4 line the Email plugin actually ships, which is what the transport test is for.",
    "type": "project",
    "license": "MIT",
    "require-dev": {
        "phpunit/phpunit": "^11.5",
        "psr/http-message": "^1.1 || ^2.0",
        "symfony/http-client": "^5.4",
        "symfony/mailer": "^5.4",
        "symfony/mime": "^5.4"
    },
    "autoload-dev": {
        "psr-4": {
            "Grav\\Plugin\\EmailResend\\": "../classes/",
            "Grav\\Plugin\\EmailResend\\Tests\\": "./",
            "Symfony\\Component\\Mailer\\Bridge\\Resend\\": "../vendor/symfony/resend-mailer/"
        }
    },
    "config": {
        "sort-packages": true
    }
}
